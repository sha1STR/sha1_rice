source = /home/sha1/.config/hypr/variables.conf

source = /home/sha1/.config/hypr/autostart.conf

general {
    gaps_in = 5
    gaps_out = 48, 8, 8, 8 # for eww

    border_size = 2

    col.active_border = rgba(ffffffff) rgba(c8c8c8ff) 45deg
    col.inactive_border = rgba(1e1e1eff)

    resize_on_border = true

    allow_tearing = true 

    layout = dwindle
}

decoration {
    rounding = 10
    rounding_power = 2

    active_opacity = 0.9
    inactive_opacity = 0.9
    fullscreen_opacity = 1.0

    layerrule = blur, waybar
    layerrule = blur, wofi 
    layerrule = blur, eww
    layerrule = ignorezero, wofi

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

animations {
    enabled = yes, please :)

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint, fade
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, easeOutQuint, slide
    animation = workspacesIn, 1, 1.21, easeOutQuint, slide
    animation = workspacesOut, 1, 1.94, easeOutQuint, slide
}

dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

master {
    new_status = master
}

misc {
    vfr = true
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


input {
    kb_layout = us, ua, ru
    kb_variant = ,,
    kb_model =
    kb_options = grp:alt_shift_toggle
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
    accel_profile = flat
    touchpad {
        natural_scroll = true
    }
}

device {
    name = msft0001:00-04f3:31be-touchpad
    sensitivity = 1.5
}

source = /home/sha1/.config/hypr/keybinds.conf

source = /home/sha1/.config/hypr/windowrules.conf

source = /home/sha1/.config/hypr/mpvpaper.conf

source = /home/sha1/.config/hypr/layerrules.conf